# Vulnerable .env file — contains a committed plaintext API key
# This file should NOT be committed to version control.

# Stripe live secret key — triggers config-plaintext-secret
API_KEY=test_sec_key_4xK9mNqRp2vBn7tLwS3cur3P4ssw0rdXq8mZ

# Another plaintext credential
DATABASE_PASSWORD=Sup3rS3cr3tPa55word!

# This is fine — it's a non-sensitive variable
APP_PORT=3000
APP_ENV=production
