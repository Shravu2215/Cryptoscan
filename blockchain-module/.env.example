# RPC endpoint for the target network.
# Local dev: leave as default (matches `npm run node`)
# Sepolia demo: get a free endpoint from Alchemy or Infura
RPC_URL=http://127.0.0.1:8545
SEPOLIA_RPC_URL=

# Throwaway testnet wallet private key — NEVER a real/mainnet wallet.
# Fund this address from a Sepolia faucet before the demo.
PRIVATE_KEY=
